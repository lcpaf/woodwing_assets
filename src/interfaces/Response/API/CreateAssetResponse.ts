export interface CreateAssetResponse {
    highlightedText?: string;
    id: string;
    metadata?: Record<string, any>;
    originalUrl?: string;
    permissions?: string;
    previewUrl?: string;
    thumbnailUrl?: string;
    uploadUrl?: string;
}
